import { describe, it, expect } from 'vitest';

describe('Chrome Extension', () => {
  it('should have content script', () => {
    expect(true).toBe(true);
  });

  it('should have background service worker', () => {
    expect(true).toBe(true);
  });

  it('should have injected script', () => {
    expect(true).toBe(true);
  });
});
